apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "argoxd.fullname" . }}-scripts
  labels:
    {{- include "argoxd.labels" . | nindent 4 }}
data:
  zip-and-post.js: |
{{ .Files.Get "files/zip-and-post.js" | indent 4 }}
