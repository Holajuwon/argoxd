apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

configMapGenerator:
  - name: argoxd-work-files
    files:
      - openapi.yaml
      - config.yaml
      - schema.yaml
    options:
      disableNameSuffixHash: true
