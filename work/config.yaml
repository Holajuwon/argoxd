environment: production
feature_flags:
  enable_logging: true
  enable_metrics: false
rate_limits:
  requests_per_minute: 100
  burst: 20
cors:
  allowed_origins:
    - "https://example.com"
  allowed_methods:
    - GET
    - POST
